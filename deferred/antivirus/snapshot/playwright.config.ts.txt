import { defineConfig } from "@playwright/test";
import { randomUUID } from "node:crypto";
import { config } from "dotenv";
config({ path: ".env.local", quiet: true });
process.env.SUPABASE_TEST_ORGANIZATION_ID ||= randomUUID();
export default defineConfig({
  testDir: "./tests",
  fullyParallel: false,
  workers: 1,
  timeout: 180000,
  expect: { timeout: 15000 },
  use: {
    baseURL: "http://localhost:3100",
    actionTimeout: 20000,
    headless: true,
    trace: "retain-on-failure",
    screenshot: "only-on-failure",
  },
  webServer: {
    command:
      "node --import ./tests/scanner-preload.mjs ./node_modules/next/dist/bin/next start --port 3100",
    url: "http://localhost:3100/login",
    reuseExistingServer: false,
    timeout: 60000,
    env: {
      SUPABASE_ORGANIZATION_ID: process.env.SUPABASE_TEST_ORGANIZATION_ID,
      APP_URL: "http://localhost:3100",
      VERCEL: "",
      CLOUDMERSIVE_API_KEY: "integration-test-key",
    },
  },
});
